Here is **CODE + COMMENTS + OUTPUT + EXAMPLE FOR EVERY SUBPOINT** for your topic:

# ✅ **LOOPS IN JAVA (COMPLETE WITH EXAMPLES + OUTPUT)**

We will cover:

1. **for loop**
2. **while loop**
3. **do-while loop**
4. **nested loops**
5. **break**
6. **continue**
7. **enhanced for loop (for-each)**

Everything with **code + comments + output**.

---

# 🟩 **1. for Loop**

Used when you know how many times you want to repeat.

```java
public class ForLoopDemo {
    public static void main(String[] args) {

        // print numbers 1 to 5
        for (int i = 1; i <= 5; i++) {
            System.out.println(i);
        }
    }
}
```

### **OUTPUT**

```
1
2
3
4
5
```

---

# 🟩 **2. while Loop**

Used when the number of iterations is NOT known.

```java
public class WhileDemo {
    public static void main(String[] args) {

        int i = 1;

        while (i <= 5) {
            System.out.println(i);
            i++; // increment
        }
    }
}
```

### **OUTPUT**

```
1
2
3
4
5
```

---

# 🟩 **3. do-while Loop**

Runs **at least once**, even if condition is false.

```java
public class DoWhileDemo {
    public static void main(String[] args) {

        int i = 1;

        do {
            System.out.println(i);
            i++;
        } while (i <= 5);
    }
}
```

### **OUTPUT**

```
1
2
3
4
5
```

---

# 🟩 **4. Nested Loops**

Loop inside another loop.

```java
public class NestedLoopDemo {
    public static void main(String[] args) {

        // Printing a 3x3 star pattern
        for (int i = 1; i <= 3; i++) {           // outer loop
            for (int j = 1; j <= 3; j++) {       // inner loop
                System.out.print("* ");
            }
            System.out.println(); // new line
        }
    }
}
```

### **OUTPUT**

```
* * *
* * *
* * *
```

---

# 🟩 **5. break Statement**

Used to **stop the loop immediately**.

```java
public class BreakDemo {
    public static void main(String[] args) {

        for (int i = 1; i <= 5; i++) {

            if (i == 3) {
                break;  // stops loop when i=3
            }

            System.out.println(i);
        }
    }
}
```

### **OUTPUT**

```
1
2
```

---

# 🟩 **6. continue Statement**

Skips the current iteration and continues loop.

```java
public class ContinueDemo {
    public static void main(String[] args) {

        for (int i = 1; i <= 5; i++) {

            if (i == 3) {
                continue; // skip printing 3
            }

            System.out.println(i);
        }
    }
}
```

### **OUTPUT**

```
1
2
4
5
```

---

# 🟩 **7. Enhanced For Loop (for-each)**

Used for arrays and collections.

```java
public class ForEachDemo {
    public static void main(String[] args) {

        int[] arr = {10, 20, 30, 40};

        for (int num : arr) {  // for-each loop
            System.out.println(num);
        }
    }
}
```

### **OUTPUT**

```
10
20
30
40
```

---

# 🎉 Completed: All loops in Java with code + comments + output.

---

### 👉 Send your next topic (arrays / functions / patterns / operators / OOP etc.)

I’ll give full **code + explanation + output**.
